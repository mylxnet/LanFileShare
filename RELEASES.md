# 📦 LanFileShare 发布记录

> 手动分发模式：每个版本对应一个 ZIP 文件，互不覆盖。

---

## v1.1.0 — 稳定性与安全修复

**发布日期**：2026-09-18

### 🐛 修复
- 🇨🇳 **中文文件名不再变乱码**：HTTP 请求头与 multipart part 头改用 UTF-8 解码，`测试文档.pdf` 等中文名按原名落盘（旧版全部变成 `____.pdf`）
- 🛡 **目录穿越防御**：设备名归一化后校验最终路径仍在保存根内；`..`、`.` 等纯点设备名被明确拒绝（旧版 `X-Device-Id: ..` 可把文件写到保存根之外）
- 🔗 **带查询串的 URL 不再 404**：路由前剥离 query string（二维码工具常给链接追加参数，旧版扫码后白屏）
- 🚫 **超限/异常请求明确报错**：超过 256MB、截断请求、超长 part 头均返回 400 JSON，不再静默断连让手机端只显示"网络错误"
- 🧹 **文件名清理**：去控制字符、非法字符替换、长度限制；支持 RFC 5987 `filename*`（浏览器非 ASCII 文件名的另一种编码）
- 🔁 **并发同名上传**：`FileMode.CreateNew` + 序号重试，两个并发同名请求都能成功落盘
- 💥 **点击"关于"不再异常**：托盘"关于"弹窗与全局异常弹窗显式指定 owner 窗口（主窗口隐藏到托盘时回退 ownerless），且异常现在会记入日志

### ⚡ 改进
- 🌊 **真流式接收**：multipart 边收边解析（固定 1MB 滑动窗口），文件体逐块写盘——移除旧版 200MB 整包缓存与硬上限，任意请求大小内存占用恒定
- 🗂 **白名单与页面选择器同步**：`.md/.rtf/.csv/.7z/.bmp/.webp/.heic/.tiff` 服务端允许、页面 `accept` 同步放行（`AllowedExtensions.HtmlAccept` 单一来源）
- 🌐 **IP 发现统一**：`IpDiscovery` 过滤 npcap/vmnet/Hyper-V/WSL 虚拟网卡，三处实现合并为单一事实来源，二维码 URL 更可靠
- 🧪 **零依赖端到端自测**：`SelfTest/` 工程 32 项用例真实 socket 打真实服务（`dotnet run --project SelfTest`），覆盖流式边界、穿越防御、并发冲突、截断恢复

### 🛠 技术
- .NET 8 + WPF（自包含发布）
- 自研 `TcpHttpServer`：TcpListener 用户态实现 HTTP（弃用 HttpListener，规避 http.sys 静默绑定失败）
- `FileReceiver` 流式 multipart 状态机解析
- QRCoder 生成二维码
- Hardcodet.NotifyIcon.Wpf 实现系统托盘

### ⚠️ 已知限制
- 单上传请求体上限 256MB（`FileReceiver.MaxBodyBytes`，超限明确返回 400）
- HTTP 协议不带加密，仅适合局域网/家庭环境
- 单个 EXE 未签名，部分杀软首次扫描可能延迟几秒

---

## v1.0.0 — 首发

**发布日期**：2024-12-25
**分发件**：`LanFileShare-v1.0.0-win-x64.zip` (≈5.6 MB)

### ✨ 新功能
- 📲 启动应用显示二维码，手机扫码即可上传图片/文档
- 📁 自动按手机分组（用户1/用户2/...）+ 按上传日期分子目录
- 🔁 同名文件自动重命名（`(1)`、`(2)` 形式），永不覆盖
- 🪟 主窗口关闭后最小化到托盘，右键"退出"才真正结束
- 🔌 自动选择 50000-50999 空闲端口（共 1000 个候选，几乎用不完），端口被占时自动顺延到下一个
- 🚫 仅图片/文档可上传（HTML accept + 服务端白名单双重把关）
- 💾 保存路径自动记忆，下次启动自动填充（路径丢失时提示重选）

### 🛠 技术
- .NET 8 + WPF（自包含发布）
- 内置 HttpListener 提供上传端点
- QRCoder 生成二维码
- WinForms NotifyIcon 实现系统托盘

### ⚠️ 已知限制
- 单上传请求体软上限 4 GB（受 .NET 单 MemoryStream 制约）
- HTTP 协议不带加密，仅适合局域网/家庭环境
- 单个 EXE 未签名，部分杀软首次扫描可能延迟几秒
