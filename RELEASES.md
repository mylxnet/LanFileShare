# 📦 LanFileShare 发布记录

> 手动分发模式：每个版本对应一个 ZIP 文件，互不覆盖。

---

## v1.0.0 — 首发

**发布日期**：2024-12-25
**分发件**：`LanFileShare-v1.0.0-win-x64.zip` (≈5.6 MB)

### ✨ 新功能
- 📲 启动应用显示二维码，手机扫码即可上传图片/文档
- 📁 自动按手机分组（用户1/用户2/...）+ 按上传日期分子目录
- 🔁 同名文件自动重命名（`(1)`、`(2)` 形式），永不覆盖
- 🪟 主窗口关闭后最小化到托盘，右键"退出"才真正结束
- 🔌 自动选择 9000-9100 空闲端口，零冲突
- 🚫 仅图片/文档可上传（HTML accept + 服务端白名单双重把关）
- 💾 保存路径自动记忆，下次启动自动填充（路径丢失时提示重选）

### 🛠 技术
- .NET 8 + WPF（自包含发布）
- 内置 HttpListener 提供上传端点
- QRCoder 生成二维码
- WinForms NotifyIcon 实现系统托盘

### ⚠️ 已知限制
- 单上传请求体软上限 4 GB（受 .NET 单 MemoryStream 制约）
- HTTP 协议不带加密，仅适合局域网/家庭环境
- 单个 EXE 未签名，部分杀软首次扫描可能延迟几秒
